sudo: false

language:
  - cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.7
    packages:
    - gcc-4.9
    - g++-4.9
    - clang-3.7

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ./src/main/cpp/tests/runUnitTests

notifications:
  email: false